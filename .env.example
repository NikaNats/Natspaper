# ========================================
# Environment Configuration Example
# Copy this file to .env.local and fill in your values
# ⚠️  NEVER commit .env.local to version control
# ========================================

# Website Configuration
# Replace with your production domain
SITE_WEBSITE=https://nika-natsvlishvili.dev/

# Google Site Verification (Optional)
# Used for Google Search Console verification
# Get this from: https://search.google.com/search-console
PUBLIC_GOOGLE_SITE_VERIFICATION=

# ========================================
# Sentry Configuration (Error Tracking & Monitoring)
# ⚠️  SECURITY: Never hardcode DSN in source code
# ========================================

# Sentry Auth Token (BUILD TIME - for source map uploads)
# Get your auth token from: https://sentry.io/settings/account/api/auth-tokens/
# Required for: npm run build (Sentry integration uploads source maps)
# Scope: `project:releases` and `org:read`
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here

# Sentry DSN for server-side error tracking (RUNTIME)
# Get from: https://sentry.io/settings/[org]/projects/[project]/keys/
# This is the private DSN - use for server-side only
# Format: https://[key]@[domain].ingest.[region].sentry.io/[project-id]
SENTRY_DSN=

# Sentry DSN for client-side error tracking (RUNTIME)
# Get from: https://sentry.io/settings/[org]/projects/[project]/keys/
# This is a separate public DSN for browser/client-side errors
# Format: https://[key]@[domain].ingest.[region].sentry.io/[project-id]
# ⚠️  Use a different DSN than SENTRY_DSN for better security
PUBLIC_SENTRY_DSN=

# Sentry Trace Sample Rate (0.0 - 1.0)
# Controls what % of transactions are sent to Sentry
# 0.1 = 10% (recommended for production to reduce noise)
# 1.0 = 100% (use only for development/debugging)
SENTRY_TRACE_SAMPLE_RATE=0.1

# Build Environment
# Values: development, production
NODE_ENV=production

# Build Metadata (Optional)
BUILD_TIMESTAMP=
BUILD_VERSION=

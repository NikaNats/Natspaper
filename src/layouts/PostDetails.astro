---
// src/layouts/PostDetails.astro
import { render, type CollectionEntry } from "astro:content";
import { SITE, FEATURES } from "@/config";
import { getI18n } from "@/i18n";
import { type Lang } from "@/i18n/config";
import {
  resolveOgImageUrl,
  getAdjacentPosts,
  getReadingTimeDisplay,
  getPostUrl,
} from "@/utils/post";
import { PostRepository } from "@/utils/post/repository";
import type { BreadcrumbItem } from "@/types";

// Components
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/layout/Header.astro";
import Footer from "@/components/layout/Footer.astro";
import Breadcrumb from "@/components/ui/Breadcrumb.astro";
import PostHero from "@/components/post/PostHero.astro";
import PostContent from "@/components/post/PostContent.astro";
import PostFooter from "@/components/post/PostFooter.astro";
import TableOfContents from "@/components/post/TableOfContents.astro";
import MobileTableOfContents from "@/components/post/MobileTableOfContents.astro";
import SeriesBox from "@/components/post/SeriesBox.astro";
import Bibliography from "@/components/post/Bibliography.astro";
import ProvenanceBlock from "@/components/post/ProvenanceBlock.astro";

export interface Props {
  post: CollectionEntry<"blog">;
  posts: CollectionEntry<"blog">[];
  isFallback?: boolean;
  originalLocale?: Lang;
}

const { post, posts, isFallback = false, originalLocale } = Astro.props;
const {
  title,
  author,
  description,
  ogImage: initOgImage,
  canonicalURL: frontmatterCanonical,
  pubDatetime,
  modDatetime,
  timezone,
  tags,
  hideEditPost,
} = post.data;

const { Content, headings } = await render(post);

// 1. Data Preparation
const locale = (Astro.currentLocale as Lang) || "en";
const { t, tTag } = getI18n(locale);
const readingTimeText = getReadingTimeDisplay(post.body || "");

// Dynamic OG images are keyed by the SOURCE locale of the entry
const ogImage = resolveOgImageUrl(
  initOgImage,
  post.slug,
  Astro.url.origin,
  isFallback && originalLocale ? originalLocale : locale
);

// 2. Fetch Series Data
const seriesParts = post.data.series
  ? await PostRepository.getSeries(post.data.series.id, locale)
  : [];

// 3. Breadcrumb Navigation Logic
const postsHref = locale === "en" ? "/en/posts" : `/${locale}/posts`;
const breadcrumbItems: BreadcrumbItem[] = [
  { label: t("nav.home"), href: `/${locale}` },
  { label: t("nav.posts"), href: postsHref },
];

if (post.data.series) {
  const seriesTitle =
    post.data.series.title.length > 30
      ? post.data.series.title.substring(0, 27) + "..."
      : post.data.series.title;
  breadcrumbItems.push({ label: seriesTitle });
}
breadcrumbItems.push({ label: title });

// 4. Navigation Override (Prev/Next logic)
let { previous: prevPost, next: nextPost } = getAdjacentPosts(posts, post.id);

if (seriesParts.length > 0) {
  const idx = seriesParts.findIndex(p => p.post.id === post.id);
  if (idx > 0 && idx < seriesParts.length) {
    const p = seriesParts[idx - 1]?.post;
    if (p) {
      prevPost = { id: p.id, title: p.data.title, filePath: p.filePath };
    }
  }
  if (idx >= 0 && idx < seriesParts.length - 1) {
    const n = seriesParts[idx + 1]?.post;
    if (n) {
      nextPost = { id: n.id, title: n.data.title, filePath: n.filePath };
    }
  }
}

// 5. Config & Helpers
const editPostConfig = FEATURES.editPost;
const showEditPost = !!(
  editPostConfig.enabled &&
  !hideEditPost &&
  editPostConfig.url
);
const currentYear = new Date().getFullYear();
const copyrightText = `Copyright © ${currentYear} • ${t("footer.copyright")}`;

// 6. Advanced SEO (CreativeWorkSeries)
const seriesSchema = post.data.series
  ? {
      "@type": "CreativeWorkSeries",
      name: post.data.series.title,
      hasPart: seriesParts.map(p => ({
        "@type": "BlogPosting",
        headline: p.post.data.title,
        url: new URL(getPostUrl(locale, p.post.slug), SITE.website).href,
      })),
    }
  : null;

// 7. SEO: Handle Fallback Canonical + decoupled chrome vs body language
const canonicalURL =
  isFallback && originalLocale
    ? new URL(getPostUrl(originalLocale, post.slug), SITE.website).href
    : frontmatterCanonical;

// Chrome language (UI) vs body language (document semantics)
const toBcp47 = (l: string) => (l === "ka" ? "ka-GE" : "en-US");
const contentLang = toBcp47(
  isFallback && originalLocale ? originalLocale : locale
);
const docLang = isFallback ? "en-US" : toBcp47(locale);
const uiLangTag = toBcp47(locale);
---

<Layout
  title={`${title} | ${SITE.title}`}
  author={author}
  description={description}
  pubDatetime={pubDatetime}
  modDatetime={modDatetime}
  canonicalURL={canonicalURL}
  ogImage={ogImage}
  tags={tags}
  wordCount={post.body?.split(/\s+/).length}
  scrollSmooth
  seriesSchema={seriesSchema}
  noindex={isFallback}
  htmlLang={docLang}
>
  <Header />

  <div class="article-layout">
    <!-- Desktop Sidebar (TOC) -->
    <aside class="article-sidebar" aria-label="Table of contents">
      <div class="article-sidebar__inner">
        <TableOfContents {headings} />
      </div>
    </aside>

    <!-- Main Article Content -->
    <main id="main-content" class="article-container">
      <!-- Breadcrumb Navigation -->
      <Breadcrumb items={breadcrumbItems} class="mb-4" />

      <!-- Language-availability notice: role=note landmark, tagged with the UI locale -->
      {
        isFallback && (
          <aside
            role="note"
            aria-label="Language availability notice"
            lang={uiLangTag}
            class="fallback-disclaimer mx-auto my-4 mb-8 max-w-app rounded-lg border border-border bg-subtle p-4 text-sm"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="size-5 shrink-0"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <circle cx="12" cy="12" r="10" />
              <line x1="12" y1="16" x2="12" y2="12" />
              <line x1="12" y1="8" x2="12.01" y2="8" />
            </svg>
            <p class="font-semibold text-foreground">
              {locale === "ka"
                ? "სტატია ხელმისაწვდომია მხოლოდ ინგლისურ ენაზე"
                : "Article only available in English"}
            </p>
            <p class="mt-1 text-text-secondary">
              {locale === "ka"
                ? "თქვენ იმყოფებით ქართულენოვან ინტერფეისში, თუმცა წინამდებარე პუბლიკაცია წარმოდგენილია ორიგინალ ენაზე."
                : "Site navigation remains in your selected language, but the body content is served in the original language."}
            </p>
          </aside>
        )
      }

      <PostHero
        slug={post.slug}
        {title}
        {pubDatetime}
        {modDatetime}
        {timezone}
        {readingTimeText}
      />

      <!-- Mobile Table of Contents (mirrors the 81rem rail threshold: no TOC gap) -->
      <div class="block min-[81rem]:hidden">
        <MobileTableOfContents headings={headings} />
      </div>

      {
        seriesParts.length > 0 && (
          <SeriesBox
            seriesParts={seriesParts}
            currentPostId={post.id}
            locale={locale}
          />
        )
      }

      <PostContent contentLang={contentLang}>
        <Content />
      </PostContent>

      {
        post.data.references && post.data.references.length > 0 && (
          <Bibliography references={post.data.references} locale={locale} />
        )
      }

      <ProvenanceBlock
        title={title}
        author={author}
        pubDate={pubDatetime}
        modDate={modDatetime}
        canonicalUrl={typeof canonicalURL === "string"
          ? canonicalURL
          : ((canonicalURL as unknown as URL)?.toString?.() ??
            new URL(Astro.url.pathname, SITE.website).href)}
        slug={(post.slug ?? post.id).replace(/\//g, "_")}
      />

      <PostFooter
        {tags}
        {showEditPost}
        {editPostConfig}
        {post}
        {prevPost}
        {nextPost}
        {tTag}
        locale={Astro.currentLocale}
      />
    </main>
  </div>

  <Footer {copyrightText} />
</Layout>

<style>
  .article-layout {
    position: relative;
    width: 100%;
    max-width: calc(var(--main-width) + 2rem);
    margin-inline: auto;
    padding-inline: 1rem;
    padding-block: 2rem 3rem;
  }

  @media (min-width: 752px) {
    .article-layout {
      max-width: var(--main-width);
      padding-inline: 0;
    }
  }

  .article-container {
    width: 100%;
    min-width: 0;
  }

  .article-sidebar {
    display: none;
  }

  .article-sidebar__inner {
    position: sticky;
    top: calc(var(--header-height, 64px) + 1.5rem);
    opacity: 1;
    transition: opacity 0.2s ease;
  }

  /* Only mount TOC when viewport can support layout without narrowing prose (81rem = 1296px) */
  @media (min-width: 81rem) {
    .article-sidebar {
      display: block;
      position: absolute;
      top: 2rem;
      right: 100%;
      width: var(--rail-width, 14rem);
      height: 100%;
      margin-right: var(--rail-gap, 2.5rem);
    }
    .article-sidebar__inner {
      position: sticky;
      top: 6rem;
      width: var(--rail-width, 14rem);
      height: fit-content;
      max-height: calc(100vh - 8rem);
      overflow-y: auto;
      overscroll-behavior: contain;
    }
  }

  @media (min-width: 100rem) {
    .article-sidebar {
      width: 16rem;
      margin-right: 4rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .article-sidebar__inner {
      transition: none;
    }
  }
</style>

<script>
  import { featureManager } from "@/utils/features/FeatureManager";

  const init = () => {
    featureManager.initializeFeatures();
    if (document.documentElement.scrollTop > 0)
      window.scrollTo({ left: 0, top: 0, behavior: "instant" });
  };

  const cleanup = () => featureManager.cleanupFeatures();

  document.addEventListener("astro:page-load", () =>
    featureManager.initializeFeatures()
  );
  document.addEventListener("astro:after-swap", init);
  document.addEventListener("astro:before-swap", cleanup);
</script>
